name: Create and Delete File

on:
  schedule:
    - cron: "1 0 * * *"
  workflow_dispatch:

jobs:
  createAndDeleteFile:
    runs-on: ubuntu-latest

    steps:
    - name: Use Node.js 16
      uses: actions/setup-node@v4
      with:
        node-version: 16

    - name: Go to the Hacker-Bug-Hunter/
      run: cd ../..
      
    - name: Create Empty File
      run: touch .py

    - name: List Files
      run: ls

    - name: Wait for 30 seconds
      run: sleep 30

    - name: Delete File
      run: rm .py

    - name: List Files After Deletion
      run: ls
