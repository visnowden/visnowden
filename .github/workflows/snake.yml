name: Generate Animation

on:
  schedule:
    - cron: "2 0 * * *"
  workflow_dispatch:
jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate GitHub Contribution Grid Snake SVG
        uses: Platane/snk@v3
        with:
          github_user_name: visnowden
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
      - name: linux
        run: pwd; echo; ls -a; echo; ls -a dist;
      - name: Move SVG files to custom directory and Commit and push changes
        run: |
          pwd
          git config user.name "visnowden"
          pwd
          git clone https://visnowden:${{ secrets.snake }}@github.com/visnowden/visnowden
          pwd
          echo
          ls -la
          echo
          cd visnowden
          git remote set-url origin https://github.com/visnowden/visnowden.git
          echo
          ls -la
          echo
          ls -la .git
          echo
          git branch -r
          echo
          cat .git/config
          echo
          mkdir snake
          cp ../dist/* snake
          git switch -c output
          echo
          git branch -r
          echo
          git add snake
          echo
          git commit -m "Update snakes's SVG files to output"
          git push -u origin output
